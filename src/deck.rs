use crate::card::Card;

struct Deck {
    cards: Vec<Card>
}

impl Deck {
    pub fn new() -> Self {
        todo!()
    }

    pub fn new_multi(count: u32) -> Self {
        todo!()
    }

    pub fn shuffle(&self) {

    }
}